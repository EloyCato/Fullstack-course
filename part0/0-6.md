sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: HTTP 201(content created)
    deactivate server

    Note right of browser: The browser executes the JavaScript code to send the note to the server <br> and push the new note to the list of notes and renders it locally