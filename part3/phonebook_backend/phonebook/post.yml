info:
  name: post
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/api/persons
  body:
    type: json
    data: |-
      {
        "name":"aaaa",
        "number":"123422"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
