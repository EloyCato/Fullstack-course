info:
  name: get note
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/notes/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​